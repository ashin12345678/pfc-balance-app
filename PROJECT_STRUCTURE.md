# PFCバランス管理アプリ - プロジェクト構造

```
pfc-balance-app/
├── 📁 app/                              # Next.js App Router
│   ├── 📁 (auth)/                       # 認証関連ルートグループ
│   │   ├── login/
│   │   │   └── page.tsx                 # ログインページ
│   │   ├── register/
│   │   │   └── page.tsx                 # 新規登録ページ
│   │   └── layout.tsx                   # 認証レイアウト
│   │
│   ├── 📁 (dashboard)/                  # メインアプリルートグループ
│   │   ├── dashboard/
│   │   │   └── page.tsx                 # ダッシュボード（日次サマリー）
│   │   ├── meals/
│   │   │   ├── page.tsx                 # 食事一覧
│   │   │   ├── new/
│   │   │   │   └── page.tsx             # 食事登録（AI解析入力）
│   │   │   └── [id]/
│   │   │       └── page.tsx             # 食事詳細・編集
│   │   ├── scan/
│   │   │   └── page.tsx                 # バーコードスキャン
│   │   ├── history/
│   │   │   └── page.tsx                 # 履歴・グラフ表示
│   │   ├── profile/
│   │   │   └── page.tsx                 # プロフィール設定
│   │   └── layout.tsx                   # ダッシュボードレイアウト
│   │
│   ├── 📁 api/                          # API Routes
│   │   ├── 📁 ai/
│   │   │   ├── analyze/
│   │   │   │   └── route.ts             # AI食事解析エンドポイント
│   │   │   └── advice/
│   │   │       └── route.ts             # パーソナルアドバイス生成
│   │   ├── 📁 barcode/
│   │   │   └── route.ts                 # バーコード→栄養情報取得
│   │   ├── 📁 meals/
│   │   │   ├── route.ts                 # CRUD操作
│   │   │   └── [id]/
│   │   │       └── route.ts             # 個別食事操作
│   │   └── 📁 summaries/
│   │       └── route.ts                 # 日次・週次サマリー取得
│   │
│   ├── globals.css                      # グローバルスタイル
│   ├── layout.tsx                       # ルートレイアウト
│   └── page.tsx                         # ランディングページ
│
├── 📁 components/                       # UIコンポーネント
│   ├── 📁 ui/                           # Shadcn UIベースコンポーネント
│   │   ├── button.tsx
│   │   ├── card.tsx
│   │   ├── input.tsx
│   │   ├── dialog.tsx
│   │   ├── form.tsx
│   │   ├── toast.tsx
│   │   └── ...
│   │
│   ├── 📁 features/                     # 機能別コンポーネント
│   │   ├── 📁 meals/
│   │   │   ├── MealInputForm.tsx        # AI解析用テキスト入力
│   │   │   ├── MealCard.tsx             # 食事カード表示
│   │   │   ├── MealList.tsx             # 食事リスト
│   │   │   └── NutritionBadge.tsx       # PFC表示バッジ
│   │   │
│   │   ├── 📁 scanner/
│   │   │   ├── BarcodeScanner.tsx       # バーコードスキャナー
│   │   │   └── ScanResult.tsx           # スキャン結果表示
│   │   │
│   │   ├── 📁 charts/
│   │   │   ├── DailyPFCChart.tsx        # 日別PFCチャート
│   │   │   ├── WeeklyTrendChart.tsx     # 週間トレンド
│   │   │   ├── GoalProgressChart.tsx    # 目標達成度
│   │   │   └── CalorieGauge.tsx         # カロリーゲージ
│   │   │
│   │   ├── 📁 dashboard/
│   │   │   ├── DailySummary.tsx         # 日次サマリー
│   │   │   ├── QuickAddButton.tsx       # クイック追加ボタン
│   │   │   └── AdviceCard.tsx           # AIアドバイス表示
│   │   │
│   │   └── 📁 profile/
│   │       ├── ProfileForm.tsx          # プロフィール編集
│   │       └── GoalSettings.tsx         # 目標設定
│   │
│   └── 📁 layout/                       # レイアウトコンポーネント
│       ├── Header.tsx                   # ヘッダー
│       ├── Sidebar.tsx                  # サイドバー（PC用）
│       ├── BottomNav.tsx                # ボトムナビ（モバイル用）
│       └── ThemeToggle.tsx              # テーマ切り替え
│
├── 📁 lib/                              # ユーティリティ・ライブラリ
│   ├── 📁 supabase/
│   │   ├── client.ts                    # Supabase クライアント初期化
│   │   ├── server.ts                    # サーバーサイドクライアント
│   │   ├── middleware.ts                # 認証ミドルウェア
│   │   └── types.ts                     # データベース型定義
│   │
│   ├── 📁 ai/
│   │   ├── openai.ts                    # OpenAI クライアント設定
│   │   ├── prompts.ts                   # AIプロンプトテンプレート
│   │   └── parsers.ts                   # AIレスポンスパーサー
│   │
│   ├── 📁 api/
│   │   ├── openFoodFacts.ts             # Open Food Facts API連携
│   │   └── types.ts                     # API型定義
│   │
│   ├── 📁 utils/
│   │   ├── nutrition.ts                 # 栄養計算ユーティリティ
│   │   ├── date.ts                      # 日付処理
│   │   └── format.ts                    # フォーマット処理
│   │
│   └── validations/
│       ├── meal.ts                      # 食事データバリデーション
│       └── profile.ts                   # プロフィールバリデーション
│
├── 📁 hooks/                            # カスタムフック
│   ├── useAuth.ts                       # 認証フック
│   ├── useMeals.ts                      # 食事データフック
│   ├── useDailySummary.ts               # 日次サマリーフック
│   ├── useBarcode.ts                    # バーコードスキャンフック
│   └── useAIAnalysis.ts                 # AI解析フック
│
├── 📁 stores/                           # 状態管理（Zustand推奨）
│   ├── useUserStore.ts                  # ユーザー状態
│   └── useMealStore.ts                  # 食事入力一時状態
│
├── 📁 types/                            # TypeScript型定義
│   ├── database.ts                      # Supabase生成型
│   ├── meal.ts                          # 食事関連型
│   ├── nutrition.ts                     # 栄養関連型
│   └── api.ts                           # APIレスポンス型
│
├── 📁 database/                         # データベース関連
│   └── schema.sql                       # SQLスキーマ定義
│
├── 📁 public/                           # 静的ファイル
│   ├── icons/                           # アプリアイコン
│   └── images/                          # 画像素材
│
├── 📁 styles/                           # スタイル関連
│   └── themes/                          # カスタムテーマ
│
├── .env.local                           # 環境変数（ローカル）
├── .env.example                         # 環境変数サンプル
├── .gitignore
├── next.config.js                       # Next.js設定
├── tailwind.config.ts                   # Tailwind CSS設定
├── tsconfig.json                        # TypeScript設定
├── package.json                         # 依存関係
└── README.md                            # プロジェクト説明
```

## 📋 ディレクトリ説明

### `/app` - Next.js App Router
- **(auth)**: ログイン・登録などの認証フロー
- **(dashboard)**: メインアプリケーション機能
- **api**: サーバーサイドAPIエンドポイント

### `/components` - UIコンポーネント
- **ui**: Shadcn UIのベースコンポーネント（再利用可能）
- **features**: 機能ごとにグループ化されたコンポーネント
- **layout**: アプリ全体のレイアウト要素

### `/lib` - ビジネスロジック
- **supabase**: データベース接続・認証
- **ai**: OpenAI連携・プロンプト管理
- **api**: 外部API（Open Food Facts等）
- **utils**: 共通ユーティリティ関数

### `/hooks` - カスタムReactフック
データ取得・状態管理のロジックをカプセル化

### `/stores` - グローバル状態管理
Zustandを使用した軽量な状態管理

### `/types` - 型定義
TypeScriptの型を一元管理

---

## 🚀 主要な依存パッケージ

```json
{
  "dependencies": {
    "next": "^14.x",
    "react": "^18.x",
    "@supabase/supabase-js": "^2.x",
    "@supabase/ssr": "^0.x",
    "ai": "^3.x",
    "openai": "^4.x",
    "html5-qrcode": "^2.x",
    "recharts": "^2.x",
    "zustand": "^4.x",
    "zod": "^3.x",
    "date-fns": "^3.x",
    "tailwindcss": "^3.x",
    "@radix-ui/react-*": "*",
    "class-variance-authority": "^0.x",
    "clsx": "^2.x",
    "tailwind-merge": "^2.x"
  }
}
```

---

## 🔑 環境変数 (.env.local)

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# OpenAI
OPENAI_API_KEY=your-openai-api-key

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

---

## 📊 データフロー概要

```
ユーザー入力
    ↓
┌─────────────────────────────────────────┐
│  テキスト入力  │  バーコードスキャン    │
└─────────────────────────────────────────┘
    ↓                    ↓
┌─────────────────────────────────────────┐
│      AI解析 (GPT-4o)                    │
│  - 食品名抽出                           │
│  - PFC・カロリー推定                    │
│  - JSON構造化                           │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│      Supabase (PostgreSQL)              │
│  - meal_logs に保存                     │
│  - daily_summaries 自動更新(トリガー)   │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│      ダッシュボード表示                 │
│  - PFCバランスグラフ                    │
│  - 目標達成率                           │
│  - AIアドバイス                         │
└─────────────────────────────────────────┘
```
